논문 구현 중 발생한 이슈

1. 데이터셋 부재
	Universidade Estadual de Campinas (UNICAMP) 내 주차장에서 촬영한 주차공간 데이터셋으로 3484장의 이미지 및 라벨링이 포함되어 있음.
	하지만 데이터셋은 공개되어 있지 않고 요청 시 일부 자료 제공 가능하다고는 함.

2. 제공 코드 없음
	데이터셋과 마찬가지로 제공되지 않으며 pre와 post의 수도 코드만 제공됨.
	구현 시 논문과 달라질 가능성 있음.

3. ROI 마스크 형식의 명확한 설명 부재
	어떤 포맷인지, 세그먼트 기준인지, YOLO label 형식과 일치하는지 직접 추정하여 구현해야 함.

4. 평가지표 기준의 불분명성
	논문 상의 Confusion Matrix 계산 방식
	• True Positives (TP): 실제로 빈 공간이고, 모델도 빈 공간이라고 정확히 예측함
	• True Negatives (TN): 실제로 차량이 있고, 모델도 차량 있음이라고 정확히 예측함
	• False Positives (FP): 실제로는 차량이 있는데, 모델이 빈 공간이라고 잘못 예측함
	• False Negatives (FN): 실제로는 빈 공간인데, 모델이 차량 있다고 잘못 예측함

	논문 상에서는 빈공간을 따로 탐지하는 것이 아니라 차량만 탐지하고 빈공간은 간접적으로 전체 공간에서 뺀 값으로 계산함.
	현재 코드 상에서는 주차된 차량의 총 대수를 탐지하고, 탐지된 차량 수는 TP, 탐지 못한 차량수는 FN(간접적으로 전제 - TP로 계산)으로 설정.

5. 상세 구현의 어려움
	본 논문에서는 다양한 환경(모바일, 데스크탑, cpu, gpu, raspberry pi 등)에서 진행.
	하지만 현실적으로 다양한 환경에서 구현이 어려움.

